# 決済の仕組みメモ

このサイトで「お金の決済が起きる場所」は、次の **4つ** です。

---

## 決済が起きる4つの場所

### 1. 入会時（`/membership/join`）
- 入会フォームから申し込むと、**入会金＋年会費を一括でクレジットカード決済**します。
- 決済が完了すると会員登録され、入会完了メールが届きます。
- このとき使ったカードは、自動引き落とし用に**保存**されます。

### 2. マイページでカード登録（登録だけ・決済は発生しない）
- 会員マイページの会員証にある「**会費のお支払いにカードを登録する**」ボタンから登録します。
- これは **登録だけ**で、その場では**お金は一切引き落とされません**（Stripe の安全な画面でカード番号を入力）。
- 登録すると、次回以降の自動引き落とし（4）の対象になります。

### 3. マイページで未納分を支払う（先日対応した分）
- **未納のある会員**が、マイページから**その場でカード決済**して未納分を支払います。
- 未納が複数年度ある場合は、**古い年度から**支払います。
- 支払ったカードは保存され、以降の自動引き落とし（4）の対象になります。

### 4. 1月の自動引き落とし（サーバーが自動実行）
- 保存済みのカードから、**毎年 1月22日（日本時間）** にサーバーが**自動で**年会費を引き落とします。
- 事務局の操作は不要です（自動）。

---

## 自動引き落とし（4）の対象になる人

- **(2) または (3) でカードを保存した人だけ**が、自動引き落とし（4）の対象です。
- **CSS（口座振替）の人は、自動引き落としの対象外**です（口座振替で別途お支払い）。

| 支払い方法 | 1月の自動引き落とし |
|---|---|
| クレジットカード（登録済み） | ✅ 対象 |
| クレジットカード（未登録） | ❌ 対象外（先にカード登録が必要） |
| CSS（口座振替） | ❌ 対象外 |

---

## 補足
- カード情報そのものは **Stripe（決済代行）側**に安全に保管され、サイトのデータベースには保存していません（保存しているのは「どの会員がどのStripe顧客か」というひも付けIDのみ）。
- 「いつ・誰に・いくら」自動引き落としされるかは、毎年1月のタイミングでサーバーが判定して実行します。
