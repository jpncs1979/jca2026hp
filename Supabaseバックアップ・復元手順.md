# Supabase 会員データ バックアップ・復元手順

会員情報（`profiles`）と関連テーブル（`memberships` / `payments`）のバックアップと復元の手順をまとめます。
専門知識がなくても、コマンドを1行コピーして実行するだけでできます。

---

## 関連ファイルの場所（すべてプロジェクト直下 `2026clHP/` 基準）

| ファイル | 場所 | 役割 |
|---|---|---|
| 本手順書 | `Supabaseバックアップ・復元手順.md`（プロジェクト直下） | この文書 |
| バックアップ用スクリプト | `scripts/backup-supabase.mjs` | データを保存する |
| 復元用スクリプト | `scripts/restore-supabase.mjs` | データを書き戻す |
| バックアップの保存先 | `backups/日時/`（実行すると自動作成） | 出力されたデータ |
| 接続情報 | `.env.local`（プロジェクト直下） | Supabase の URL とキー |

> プロジェクト直下 = `C:\Users\…\14HP\2026clHP\` のことです。

---

## 0. 事前準備（最初の1回だけ）

- プロジェクトのフォルダ（`2026clHP`）を開きます。
- `.env.local` ファイルがあること（中に `NEXT_PUBLIC_SUPABASE_URL` と `SUPABASE_SERVICE_ROLE_KEY` が入っています）。
- PowerShell（ターミナル）でプロジェクト直下にいること。

> ⚠️ `.env.local` と `SUPABASE_SERVICE_ROLE_KEY` は強力なキーです。人に共有しないでください。

---

## 1. バックアップ（保存）する

プロジェクト直下で、次の1行を実行します。

```powershell
node --env-file=.env.local scripts/backup-supabase.mjs
```

実行すると、`backups/` の中に日時フォルダが作られ、その中に保存されます。

```
backups/2026-06-07-08-28-57/
├── profiles.json     ← 会員情報（完全データ。これが本体）
├── profiles.csv      ← 会員情報（Excelで開ける一覧）
├── memberships.json  ← 会員資格（有効期限など）
└── payments.json     ← 入金記録
```

- `profiles.json` が**完全なバックアップ**です（カード登録のひも付け `stripe_customer_id` も含みます）。
- `profiles.csv` は中身を**Excelで確認する**ための表です。
- バックアップは**お手元のPC（OneDrive内）にだけ**保存されます。Git／GitHub には上がりません（`.gitignore` で除外済み）。

### いつ取ると良いか
- 会員データを**一括更新・取り込み・一括削除する前**に必ず1回。
- 月初など、定期的に。
- 大きな作業をする前。

---

## 2. 中身を確認（読み取り）する

- **`backups/日時/profiles.csv`** を Excel でダブルクリックして開く → 会員一覧として確認できます。
- **`backups/日時/profiles.json`** はメモ帳やエディタで開くと全項目を確認できます（システムが読み戻すのはこちら）。

---

## 3. 復元（書き戻し）する

バックアップを Supabase に戻したいとき（例：誤って消した／古い状態に戻したい）は、
**復元したいバックアップフォルダを指定して**次を実行します。

```powershell
node --env-file=.env.local scripts/restore-supabase.mjs backups/2026-06-07-08-28-57
```

- `id` をキーに **upsert（あれば更新・なければ追加）** します。
- **既存の行を削除はしません**（安全側の動作）。
- `profiles` → `memberships` → `payments` の順で書き戻します。

> 復元後は、会員管理画面で件数や内容を確認してください。

---

## 4. カード登録（クレジットカード）に関する重要な注意

カード情報は **2か所** に分かれて保存されています。

1. **Supabase `profiles.stripe_customer_id`** … 「どの会員がどのStripe顧客か」というひも付け（このバックアップに含まれます）
2. **Stripe側** … 実際のカード情報（Supabaseには保存していません）

ポイント：

- `profiles` を削除すると、**1（ひも付け）が失われ**、アプリはカードで自動決済できなくなります。
- このバックアップには `stripe_customer_id` も入っているので、**復元すればひも付けも戻ります**。
- ただし**会員を再インポート（新規作成）し直した場合は、新しい行になり `stripe_customer_id` は空**になります。その場合はこのバックアップから復元するか、本人に再登録してもらう必要があります。

> 運用開始後（カード登録者が出た後）は、`profiles` の一括削除は避けてください。退会は会員詳細の「退会処理」で必要な人だけ行います。

### バックアップから復元したらカードのひも付けは戻る？

**戻ります。** 理由は次のとおりです。

- バックアップ（`profiles.json`）には `stripe_customer_id`（カードのひも付け）も含まれています。
- 復元スクリプトは**同じ会員ID（`id`）で書き戻す**ので、`stripe_customer_id` もそのまま復元されます。
- 実際のカード情報は **Stripe 側**にあり、Supabase を消しても Stripe からは消えていません。ひも付けが戻れば**自動決済も元どおり動きます**。

| 状況 | カードのひも付け |
|---|---|
| profiles を削除 → **このバックアップから復元** | ✅ 戻る（同じ `id` で書き戻すため） |
| 会員を **Excel で再インポート（新規作成）** | ❌ 戻らない（新しい `id`・`stripe_customer_id` は空） |

> 消した後の復旧は、**再インポートではなく必ず「バックアップ復元（3章）」を使う**こと。

#### おすすめの事前確認（運用開始後）
本番でカード登録者が出たら、一度 **テストモード**で次の流れを通しておくと安心です。

1. テスト用会員でカードを登録する
2. その会員の `profiles` 行を削除する
3. バックアップから復元する（3章）
4. `stripe_customer_id` が戻り、自動決済の対象に戻ることを確認する

---

## 5. 参考：他のバックアップ方法

- **Supabase 管理画面**：Table Editor で `profiles` を開き、CSV エクスポート。手軽だが1テーブルずつ・上限あり。
- **SQL Editor / pg_dump**：データベース全体の本格バックアップ（技術者向け）。

ワンコマンドで関連テーブルまでまとめて取れる **本書のスクリプト方式（1章）** が、日常運用には最もおすすめです。

---

## まとめ（早見表）

| やりたいこと | コマンド／場所 |
|---|---|
| バックアップを取る | `node --env-file=.env.local scripts/backup-supabase.mjs` |
| 中身を確認する | `backups/日時/profiles.csv` を Excel で開く |
| 復元する | `node --env-file=.env.local scripts/restore-supabase.mjs backups/日時` |
